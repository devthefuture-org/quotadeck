{
  "indicator-header": {
    "type": "header",
    "description": "Panel indicators"
  },
  "display-indicators": {
    "type": "list",
    "default": [],
    "description": "Indicators shown in the panel",
    "tooltip": "Use + to add indicators. Each row shows one provider icon and quota percentage; drag or use the arrows to reorder them.",
    "height": 240,
    "columns": [
      {
        "id": "indicator",
        "title": "Provider · account · quota window",
        "type": "string",
        "default": "auto",
        "options": {
          "Automatic — tightest available": "auto"
        }
      }
    ]
  },
  "display-account": {
    "type": "generic",
    "default": "auto"
  },
  "display-window": {
    "type": "generic",
    "default": "auto"
  }
}
